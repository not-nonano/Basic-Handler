const { VoiceState } = require('discord.js')

module.exports = {
    name: 'voiceStateUpdate',
    once: false,
    /**
     * 
     * @param {VoiceState} oldState 
     * @param {VoiceState} newState 
     * @returns 
     */
    async execute(oldState, newState) {
        /**
         * Voice State event here
         * Code Stuff here
         */

    }
}