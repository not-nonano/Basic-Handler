const { Guild } = require('discord.js')

module.exports = {
    name: 'guildCreate',
    once: false,
    /**
     * 
     * @param {Guild} guild 
     * @returns 
     */
    async execute(guild) {
        /**
         * when bot joins to server
         * 
         * Code stuff here
         */
    }
}